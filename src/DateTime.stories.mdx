import { Meta, Story, Preview } from "@storybook/addon-docs/blocks";
import DateTime from "./.";

import es from "date-fns/locale/es";

<Meta title="DateTime/DateTime" component={DateTime} />

# DateTime

The `DateTime` component requires `value` and `onChange` props to work properly.

## Days

```jsx
<DateTime shouldHideInput />
```

<Preview>
  <Story name="Days shouldHideInput">
    <DateTime shouldHideInput />
  </Story>
</Preview>

## Months

```jsx
<DateTime shouldHideInput dateFormat="MM" />
```

<Preview>
  <Story name="Months shouldHideInput">
    <DateTime shouldHideInput dateFormat="MM" />
  </Story>
</Preview>

## Years

```jsx
<DateTime shouldHideInput dateFormat="YYYY" />
```

<Preview>
  <Story name="Years shouldHideInput">
    <DateTime shouldHideInput dateFormat="YYYY" />
  </Story>
</Preview>

## Time

```jsx
<DateTime shouldHideInput dateFormat={false} />
```

<Preview>
  <Story name="Time shouldHideInput">
    <DateTime shouldHideInput dateFormat={false} />
  </Story>
</Preview>
